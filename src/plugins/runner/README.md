# runner

> Complex plugin — durable run pipeline: queued → gate(budget+dedup, atomic) → execute → store → done; owns all run events

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
