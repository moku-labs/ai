# registry

> Nano plugin — dumb task→provider→handler transport; resolve() returns unknown, narrowed by each task plugin

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
