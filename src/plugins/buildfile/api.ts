/**
 * @file buildfile plugin — API factory skeleton.
 */
import type { BuildfileApi } from "./types";

/**
 * Creates the buildfile API surface.
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @example
 * ```ts
 * const api = createBuildfileApi(ctx);
 * ```
 */
export function createBuildfileApi(_ctx: unknown): BuildfileApi {
  throw new Error("not implemented");
}
