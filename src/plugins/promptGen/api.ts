/**
 * @file promptGen plugin — API factory skeleton.
 */
import type { PromptGenApi } from "./types";

/**
 * Creates the prompt-gen API surface (generate/estimate/providers).
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @example
 * ```ts
 * const api = createPromptGenApi(ctx);
 * ```
 */
export function createPromptGenApi(_ctx: unknown): PromptGenApi {
  throw new Error("not implemented");
}
