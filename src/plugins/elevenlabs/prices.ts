/**
 * @file elevenlabs bundled price table — data module (USD per character by model).
 */

/** Bundled per-character prices by model; merged with config.priceOverrides at first use. */
export const bundledPrices: Record<string, number> = {};
