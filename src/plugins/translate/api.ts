/**
 * @file translate plugin — API factory skeleton.
 */
import type { TranslateApi } from "./types";

/**
 * Creates the translate API surface (generate/estimate/providers).
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @example
 * ```ts
 * const api = createTranslateApi(ctx);
 * ```
 */
export function createTranslateApi(_ctx: unknown): TranslateApi {
  throw new Error("not implemented");
}
