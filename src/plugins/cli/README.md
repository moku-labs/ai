# cli

> Complex plugin — the `moku` command surface: mountable command tree + post-start dispatch() returning the exit-code contract

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
