/**
 * @file compose plugin — API factory skeleton.
 */
import type { ComposeApi } from "./types";

/**
 * Creates the compose API surface (compose).
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @example
 * ```ts
 * const api = createComposeApi(ctx);
 * ```
 */
export function createComposeApi(_ctx: unknown): ComposeApi {
  throw new Error("not implemented");
}
