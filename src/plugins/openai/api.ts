/**
 * @file openai provider plugin — API factory skeleton.
 */
import type { OpenaiApi } from "./types";

/**
 * Creates the openai API surface (info).
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @example
 * ```ts
 * const api = createOpenaiApi(ctx);
 * ```
 */
export function createOpenaiApi(_ctx: unknown): OpenaiApi {
  throw new Error("not implemented");
}
