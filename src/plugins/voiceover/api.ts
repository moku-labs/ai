/**
 * @file voiceover plugin — API factory skeleton.
 */
import type { VoiceoverApi } from "./types";

/**
 * Creates the voiceover API surface (generate/estimate/providers).
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @example
 * ```ts
 * const api = createVoiceoverApi(ctx);
 * ```
 */
export function createVoiceoverApi(_ctx: unknown): VoiceoverApi {
  throw new Error("not implemented");
}
