/**
 * @file narration template pack — versioned data module (NOT a plugin).
 */

/** M0 narration pack: per-provider param presets, versioned for artifact identity. */
export const narrationPack = {
  name: "narration",
  version: "1.0.0",
  values: {}
} as const;
